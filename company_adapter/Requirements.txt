# DataShield AI — Company Adapter (Layer 2)
# Install with: pip install -r requirements.txt

# Document parsing
pymupdf>=1.23.0          # PDF extraction (fitz)
python-docx>=1.1.0       # DOCX extraction
openpyxl>=3.1.2          # XLSX extraction
pandas>=2.0.0            # CSV/tabular data

# NLP & NER
spacy>=3.7.0             # NER (multilingual small model)

# Embeddings & similarity (lightweight multilingual)
sentence-transformers>=2.6.0
faiss-cpu>=1.7.4         # Fast vector search (CPU only)
scikit-learn>=1.3.0      # PCA for dimension reduction

# Utilities
tqdm>=4.66.0
numpy>=1.24.0
